#!/bin/sh

echo "🧪 Running pre-commit tests..."

# Run unit tests only (component tests require jsdom setup)
npm run test:unit
if [ $? -ne 0 ]; then
  echo "❌ Unit tests failed. Commit aborted."
  exit 1
fi

echo "✅ All tests passed!"
